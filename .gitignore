.env
*.env
max/.venv/
frontend/node_modules/
frontend/dist/
__pycache__/
*.pyc
.DS_Store
# Personal memory. conversation_log was already ignored; the archive is
# the same content just older, profile.md is a written profile of the
# user, and memory_state is local bookkeeping. This repo is public.
max/memory/conversation_log.jsonl
max/memory/conversation_archive.jsonl
max/memory/facts.md
max/memory/profile.md
max/memory/memory_state.json
.claude/settings.local.json
.claude/*.lock
max/browser_profile/
max/.composio_session.json
# Live prefs: phone number, Composio user id, local model picks.
# The committed template is max/config.example.json.
max/config.json
